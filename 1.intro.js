/* module - this is a module
const secret = "super secret"
const a = 'john'
const b = 'peter'

const sayHi = (name) => {
    console.log(`hi ${name}`)
}
*/
//const data = require('./4.alt')
//const names = require('./2.names');
//const sayHi = require('./3.utils')
const nom = require('./5.mindgre') //no need to consolelog as function is already called
//console.log(data)
//console.log(names)
//sayHi('susan')
//sayHi(names.a)

//Built in module - os , path , fs , http

